export type Products = {
    betType: string;
    results: [];
  };
  
export type Result = {
    id: string;
    startTime: string;
    tracks: [];
  };

export type DropDownOption = { 
    label: string; 
    value: string 
  };